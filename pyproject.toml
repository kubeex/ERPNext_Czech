[project]
name = "czech_localization"
authors = [
    { name = "Kuban", email = "kuban.sir@gmail.com" }
]
description = "Czech language, terminology and localization layer for ERPNext and HRMS"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core>=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
